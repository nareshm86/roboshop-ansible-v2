- name: get secret from Secrets Manager
  hosts: local
  connection: local
  tasks:
  - name: aws secret password
    debug:
    msg: "{{ lookup('amazon.aws.aws_ssm', '/roboshop/mysql/password', region='us-east-1') }}"
